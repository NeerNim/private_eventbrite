<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>List of Attendees</h1>
    <ul>
      <% @event.attendees.each do |attendee| %>
        <li>
          <h3><%= attendee.name %></h3>
        </li>
        <hr>
      <% end %>
    </ul>
  </div>
</div>
