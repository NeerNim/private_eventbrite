<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>List of Attendees</h1>
      <% @event.attendees.each do |attendee| %>
        <ol>
          <h3><li><%= attendee.name %></li></h3>
        </ol>
        <hr>
      <% end %>
  </div>
</div>
